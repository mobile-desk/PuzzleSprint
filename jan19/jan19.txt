Write a command-line application that prompts the user to specify a path to "john.doe@example.com"  and then extracts and displays that value.

The source code for multiple languages would be displayed in the following slides